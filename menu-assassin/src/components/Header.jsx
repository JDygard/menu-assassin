import React from 'react';
import { styled } from '@mui/system';
import { AppBar, Toolbar, Typography } from '@mui/material';


const Header = () => {
    return (
            <Toolbar>
                <Typography variant="h6">Menu Assassin</Typography>
            </Toolbar>
    );
};

export default Header;